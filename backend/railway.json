{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "bash -c 'python -u scripts/download_llms.py || echo LLM download skipped; python -u scripts/download_hunspell_v2.py || echo Hunspell v2 skipped; python -u -m uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000} --log-level info'",
    "healthcheckPath": "/docs",
    "healthcheckTimeout": 1200,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
