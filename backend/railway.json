{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "echo '[start] booting...'; python -u scripts/download_mistral.py || echo '[start] mistral skipped'; echo '[start] launching uvicorn'; exec python -u -m uvicorn main:app --host 0.0.0.0 --port $PORT --log-level info",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 600,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
