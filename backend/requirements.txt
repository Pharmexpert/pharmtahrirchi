fastapi==0.135.1
uvicorn[standard]==0.41.0
python-dotenv==1.2.2
python-multipart==0.0.22
pydantic==2.12.5
google-genai>=1.0.0
anthropic>=0.25.0
requests==2.32.5
openpyxl==3.1.5
xlrd>=2.0.1
beautifulsoup4>=4.12.0
python-docx==1.2.0
python-pptx>=0.6.23
pdf2docx==0.5.12
pypdf>=4.0.0
pdfplumber>=0.10.0

# Mistral-7B-Instruct-Uz: HF Inference API (no llama-cpp needed for cloud mode)
# To enable local GGUF mode later: pip install llama-cpp-python (~10 min build with cmake)
huggingface-hub>=0.20.0

# HuggingFace datasets (for Tahrirchi uz-crawl, dilmash, lutfiy, uzlib import)
datasets>=2.16.0

# Accelerate — for efficient model loading (device_map, 8-bit, low_cpu_mem_usage)
accelerate>=0.27.0

# llama-cpp-python for GGUF Q4/Q8 CPU inference (5-8 tok/sec on Railway Pro)
# Optional — only loaded when LLAMA_GGUF_PATH or MISTRAL_GGUF_PATH files exist
llama-cpp-python>=0.2.90; platform_system != "Windows"
pandas==2.2.3
numpy==2.2.5
faiss-cpu==1.13.2
transformers==5.4.0
langdetect==1.0.9
PyJWT==2.9.0
torch --index-url https://download.pytorch.org/whl/cpu

# Spellcheck (uz-hunspell full affix support)
spylls>=0.1.7

# PDF export
reportlab>=4.0.0

# Phase 6: Nightly cron scheduler
APScheduler>=3.10.0

# OCR (Phase 6) — Tesseract installed via nixpacks.toml
pytesseract>=0.3.10
Pillow>=10.0.0

# Testing
pytest>=8.0.0
httpx>=0.27.0
anyio>=4.0.0
