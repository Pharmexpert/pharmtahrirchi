# Implementation Plan: Finalizing the Pharmaceutical Linguistic Engine (Qonun Standard)

Based on the [Qonun](file:///c:/Users/Администратор/Desktop/2/Qonun) documentation, we are finalizing the "Linguistic Dashboard" and "Sayqallash" (Polishing) engine. This plan ensures that all transition to **Gemini 2.0 Flash** is complete and all premium features from the "Qonun" session are fully integrated.

## User Review Required

> [!IMPORTANT]
> **AI Migration Completion**: We will replace the last remaining **Claude** calls (in `align-document` and `split-row`) with **Gemini 2.0 Flash**. This completes the legal requirement to move away from expensive/expired APIs while keeping the "Qonun" logic.

> [!TIP]
> **Synonym Side Panel**: We will finalize the "Quick Apply" logic. Clicking a synonym in the side drawer will instantly update the document text.

## Proposed Changes

### 1. Backend: Final Gemini Integration (Phase 1)
- **[MODIFY] [main.py](file:///c:/Users/Администратор/Desktop/2/backend/main.py)**:
    - **[FIX]** `align_document`: Switch from `claude-sonnet-4-20250514` to `models/gemini-2.0-flash`.
    - **[FIX]** `split_row`: Switch from `claude-sonnet-4-20250514` to `models/gemini-2.0-flash`.
    - **[REFINE]** `get_sayqallash_prompt`: Update the Gemini prompt to be even more strict about pharmaceutical terminology (Pharmacopoeia standards) as defined in the Qonun.
    - **[REFINE]** `sayqallash`: Ensure the `confidence` score (0-100) is always parsed from the Gemini response.

### 2. Frontend: Premium UX Completion (Phase 2)
- **[MODIFY] [TableEditor.tsx](file:///c:/Users/Администратор/Desktop/2/frontend/components/TableEditor.tsx)**:
    - **Synonym Selection**: Implement the `applyVariant` logic inside the `synonymPanel` UI (currently missing/).
    - **Background Status**: Add a "Processing..." status indicator in the header that monitors if rows are being pre-polished in the background.
    - **Confidence Badges**: Ensure confidence scores from the background worker are displayed correctly in the `LangCell`.

### 3. Database & Self-Learning (Phase 3)
- **[MODIFY] [db.py](file:///c:/Users/Администратор/Desktop/2/backend/db.py)**:
    - Verify all linguistic tables (`annotated_words`, etc.) include `text_id`, `modified_by_id`, and `status`.
    - Ensure `add_sayqallash_rule` correctly indexes and migrates vectors for the 3-tier matching.

## Open Questions

1. **Alignment Batch Size**: Gemini-2.0-Flash has a very large context. Should we increase the alignment batch size in `main.py` from 4 to 10 for faster document processing?

## Verification Plan

### Automated Tests
- Run `api/align-document` with a sample payload to verify Gemini's trilingual mapping quality.
- Run `api/split-row` to verify sentence-aware splitting.

### Manual Verification
1. Open a project.
2. Click "AI Moslash" (AI Align) and check trilingual alignment.
3. Highlight a word, see synonyms in the side panel, click one, and see the text update.
4. Verify the "Background Polishing" (Red X to Green Check transition) happens automatically after upload.
