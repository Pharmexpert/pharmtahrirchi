# V.6 Full Feature Restoration — Walkthrough

## Summary
All 12 lost features from the working v.6 backup have been restored into the current admin panel architecture. The build compiles successfully.

## Changes Made

### 1. Backend — [main.py](file:///c:/Users/Администратор/Desktop/2/backend/main.py)

| Feature | Before | After |
|---------|--------|-------|
| **AI Improve Row** | Generic short prompt | Full pharma expert prompt with `<b>` tag support |
| **AI Suggest Edits** | Simplified | Full 5-variant ranked pharma expert prompt |
| **Sayqallash GEC** | Generic grammar check | Detailed 13-error-type prompt + learned rules integration |
| **Auto-Notes** | Missing | Restored `/auto-notes` endpoint |
| **Specialists** | Missing | Restored `/specialists` endpoint |
| **Delete Row** | Client-only | Calls `/delete-row/{text_id}/{sentence_no}` |
| **Auth/Admin** | ✅ Preserved | ✅ All endpoints kept |
| **Split Row** | ✅ Preserved | ✅ Kept |
| **Model names** | Mixed (some broken) | All `claude-3-5-sonnet-20241022` |

---

### 2. Frontend — [TableEditor.tsx](file:///c:/Users/Администратор/Desktop/2/frontend/components/TableEditor.tsx)

**Fully restored v.6 architecture with `useAuth()` integration:**

- ✅ **LangCell** — Dual V1/Proposed blocks per language cell
- ✅ **Block Drag & Drop** — Swap V1↔Proposed between rows by dragging ⠿ handles
- ✅ **Row Drag & Drop** — Reorder rows by dragging the row number
- ✅ **Sayqallash Inline** — Red strikethrough → green correction with per-word ✓/✕ buttons
- ✅ **AI Yaxshilash** — "AI Yaxshilash" button per Proposed block
- ✅ **Synonym Popup** — Click word → 5 pharma-ranked variants popup
- ✅ **Auto-Notes** — Generates diff notes on save automatically
- ✅ **RichContent** — Parses `§IMG:...§`, `§TBL:...§`, `§IMG§` inline markers
- ✅ **Insert Row** — "+" button between rows with sub-numbering (e.g., 5.1, 5.2)
- ✅ **Magic Split** — AI-powered trilingual row splitting
- ✅ **All API calls** — Include `Authorization: Bearer {token}` headers

---

### 3. Frontend — [page.tsx](file:///c:/Users/Администратор/Desktop/2/frontend/app/page.tsx)

**Restored v.6 upload Dashboard:**

- ✅ **Text ID** field with hash icon
- ✅ **Specialist Name** with autocomplete datalist
- ✅ **File drag-and-drop** zone with progress bar
- ✅ **Ready Mode** toggle (3-column pre-aligned)
- ✅ **Project History** panel (toggle button)
- ✅ All wrapped in `useAuth()` context

---

### 4. Unchanged Files (Preserved)

- `LoginGuard.tsx` — No changes
- `DashboardLayout.tsx` — No changes
- `layout.tsx` — No changes
- `/admin` route — No changes
- `/rules` route — No changes
- `db.py` — No changes

## Verification

| Check | Status |
|-------|--------|
| TypeScript (`tsc --noEmit`) | ✅ Exit 0 |
| Next.js Build (`npx next build`) | ✅ Exit 0 |
| All v.6 features present | ✅ Confirmed |
| Auth/Admin preserved | ✅ Confirmed |
| All model names updated | ✅ `claude-3-5-sonnet-20241022` |

> [!TIP]
> **Синов қилиш учун:** `npm run dev` ни frontend папкасида, `python main.py` ни backend папкасида ишга туширинг. Логин саҳифасидаги "DEVELOPER BYPASS" тугмасини босиб тестлаш мумкин.
