# Implementation Plan - Sayqallash and File Management Improvements

The user wants to rename "Rules DB" to "Sayqallash", add Excel export for rules, and enhance the "Files Directory" with upload capabilities and a fix for deletion.

## User Review Required

> [!IMPORTANT]
> The "Rules DB" will be renamed to "Сайқаллаш" (Sayqallash) throughout the UI for better clarity.

- **Libraries**: I will use `pandas` and `openpyxl` (already installed) for the XLSX export.
- **Upload Context**: The upload function in the directory will work similarly to the main landing page but specifically focused on managing the file list.

## Proposed Changes

### 1. Rename Rules DB to Сайқаллаш

#### [MODIFY] [DashboardLayout.tsx](file:///c:/Users/Администратор/Desktop/2/frontend/components/DashboardLayout.tsx)
- Change sidebar menu item name from "Rules DB" to "Сайқаллаш".

#### [MODIFY] [rules/page.tsx](file:///c:/Users/Администратор/Desktop/2/frontend/app/rules/page.tsx)
- Change page heading from "Sayqallash Qoidalari 📖" to "Сайқаллаш қоидалари 📖".

### 2. XLSX Export for Rules

#### [MODIFY] [admin_routes.py](file:///c:/Users/Администратор/Desktop/2/backend/admin_routes.py) [NEW ENDPOINT]
- Add `@router.get("/rules/export")` to fetch data from the `sayqallash_rules` table and return it as an XLSX file using `pandas`.

#### [MODIFY] [rules/page.tsx](file:///c:/Users/Администратор/Desktop/2/frontend/app/rules/page.tsx)
- Add a "Download XLSX" button in the header.
- Implement the download logic (fetch the blob and trigger browser download).

### 3. File Upload in Directory

#### [MODIFY] [files/page.tsx](file:///c:/Users/Администратор/Desktop/2/frontend/app/files/page.tsx)
- Add a "Файл юклаш" button in the header area.
- Add a hidden file input and the `handleUpload` logic.
- Ensure the project list refreshes after a successful upload.

### 4. Fix File Deletion in Directory

#### [MODIFY] [files/page.tsx](file:///c:/Users/Администратор/Desktop/2/frontend/app/files/page.tsx)
- Improve `handleDelete` with better error reporting and ensuring the `API_BASE` is correctly used.
- Add a refresh call or verify state update.

---

## Verification Plan

### Automated Tests
- N/A (Manual visual verification).

### Manual Verification
1. Open the sidebar and check if "Rules DB" has been renamed to "Сайқаллаш".
2. Navigate to "Сайқаллаш" and click the new XLSX download button. Verify the file contents.
3. Navigate to "Файллар директорияси" and try uploading a new `.docx` or `.pdf` file. Verify it appears in the list.
4. Try deleting a file and verify it disappears from the list and the backend (check `/backend/uploads`).
