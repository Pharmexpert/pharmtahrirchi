# Implementation Plan: Secure Upload & Unified Features

This plan resolves the "Mixed Content" blocker for GitHub Pages and implements the requested intelligence and tracking features.

## User Review Required

> [!IMPORTANT]
> **Mixed Content Fix**: The live site (HTTPS) cannot talk to your local backend (HTTP). I will establish a **Secure HTTPS Tunnel** (e.g., Localtunnel or Cloudflare). You MUST keep this tunnel running for the live site to work.
> **Deployment**: After getting the tunnel URL, I will update the frontend and push a commit to GitHub to redeploy the site.
> **Database Migration**: I will update the `alignments` table to include `specialist_name`. Existing data will be preserved.
> **Language Detection**: I am using `langdetect`. For very short documents (under 5 words), detection might be less reliable, but for typical pharma docs, it works excellently.
> **Secure Tunnel**: To fix the "Mixed Content" issue on GitHub Pages, we need a stable HTTPS tunnel. I will provide the URL after starting it.

## Proposed Changes

### 1. Database & API (Backend)

#### [MODIFY] [db.py](file:///c:/Users/Администратор/Desktop/2/backend/db.py)
- ✅ Added `specialist_name` column and migration logic.
- ✅ Updated save functions to handle `specialist_name`.
- ✅ Added `get_unique_specialists()` to fetch unique names for autocomplete.

#### [MODIFY] [main.py](file:///c:/Users/Администратор/Desktop/2/backend/main.py)
- [NEW] Add `@app.get("/specialists")` endpoint to return the unique specialists list.
- [MODIFY] Update `/upload` to accept `specialist_name` as a query parameter or form field.

### 2. Document Processing (Backend)

#### [MODIFY] [processor.py](file:///c:/Users/Администратор/Desktop/2/backend/processor.py)
- ✅ Imported `langdetect`.
- [MODIFY] Implement `detect_and_split_single_language(text)`:
  - If no 3-column table is found, detect language (EN, RU, UZ).
  - Split text into rows.
  - Map to the detected language column.
- [MODIFY] Update `process()` and `process_ready_form()` to use this fallback logic.

### 3. User Interface & Deployment (Frontend)

#### [MODIFY] [page.tsx](file:///c:/Users/Администратор/Desktop/2/frontend/app/page.tsx)
- [MODIFY] Rename toggle label to "шу ҳолатида юклаш".
- [NEW] Add mandatory "Мутахассис исми ва шарифи" input field.
- [NEW] Implement `<datalist>` for autocomplete based on names from `/specialists`.
- [MODIFY] Disable the "Processing" button unless the name field is filled.

#### [MODIFY] [GitHub Sync]
- Get the secure tunnel URL.
- Update the frontend environment configuration.
- Push to GitHub to trigger a fresh GitHub Pages build.

## Open Questions

> [!CAUTION]
> **Tunnel Persistence**: Since I am an AI agent, if my session ends, the tunnel might close. Would you like instructions on how to keep the tunnel running permanently on your PC?

### Automated Tests
- Upload a DOCX with only English text. Verify it maps to the first column.
- Upload a DOCX with only Russian text. Verify it maps to the second column.
- Verify through Postman/Curl that `/specialists` returns unique strings.

### Manual Verification
- Check the UI for the renamed toggle.
- Verify the specialist name appears in the suggestions after a successful upload and page reload.
