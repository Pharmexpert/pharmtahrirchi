# Implementation Plan: Administrative Access & Passwordless Entry

The goal is to reset the primary administrator account (`texnopharm@gmail.com`) to a known state that allows for a "passwordless" first-time login, followed by a mandatory profile setup that establishes a permanent password.

## User Review Required

> [!IMPORTANT]
> This plan involves a temporary bypass of password verification for the administrator account. This bypass will automatically be disabled once you set your password in the Profile page.

## Proposed Changes

### Backend Logic Updates

#### [MODIFY] [db.py](file:///c:/Users/Администратор/Desktop/2/backend/db.py)
- Update `verify_password` to allow login for the primary administrator if no password hash is currently stored.
- Add a utility function to reset a user's role and ID.

#### [MODIFY] [main.py](file:///c:/Users/Администратор/Desktop/2/backend/main.py)
- Ensure the login response properly handles the "no password set" state if needed.

### Database Maintenance

- Execute a SQL script to:
    1. Identify or create the user `texnopharm@gmail.com`.
    2. Set `role = 'admin'` and `status = 'approved'`.
    3. Clear `password_hash` and `salt` to enable the first-time bypass.
    4. Set the internal ID to `admin_primary` (as requested for identification).

## Open Questions

1. **Specific ID**: You mentioned "assign me an ID". I will use `admin_primary` as the internal database ID. Does this work for you, or did you have a specific character string in mind?
2. **First Login**: Once I apply these changes, you will be able to log in by entering your email and any (or no) password. After logging in, please go to the **Profile** page immediately to set your permanent password.

## Verification Plan

### Automated Tests
- I will run a script to verify the database state for `texnopharm@gmail.com` after the update.
- I will verify that `verify_password` logic correctly returns `True` for the "NULL password" state for admins.

### Manual Verification
- You will attempt to log in with `texnopharm@gmail.com` and a blank password.
- You will verify that you can reach the Dashboard and Profile pages.
- You will set a password in the Profile page and verify that you can log out and log back in with the new password.
